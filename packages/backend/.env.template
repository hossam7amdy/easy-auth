# Server
PORT=
NODE_ENV= # development | production | test
ALLOWED_CORS=

# Frontend URL
FRONTEND_URL=

# MongoDB
MONGODB_URI=

# JWT
JWT_SECRET=
JWT_EXPIRES_IN=

# Throttler (default)
THROTTLE_TTL=
THROTTLE_LIMIT=

# SMTP (Local development with Mailpit - for production, use smtp.resend.com:465)
EMAIL_FROM=
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASS=
